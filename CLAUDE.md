# Claude Code Repository Instructions

Before changing anything in this repository, read and obey the root `AGENTS.md` completely.

`AGENTS.md` is the canonical cross-agent engineering, security, validation, Git, and concurrent-development policy for this repository. Treat it as binding for Claude Code tasks.

Do not maintain a separate copy of those rules here. If `AGENTS.md` changes, follow the latest version on the branch you are working from.

When working in parallel with other agents, pay particular attention to the `Concurrent multi-agent development` section in `AGENTS.md`.
