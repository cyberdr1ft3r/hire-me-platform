# Risk Register

Last updated: 2026-09-16

| ID | Risk | Impact | Current mitigation | State |
| --- | --- | --- | --- | --- |
| R-001 | Product scope grows faster than delivery capacity. | Delays, unstable architecture, incomplete modules. | Use scoped issues, roadmap phases, explicit non-goals, and dependency gates. | Active |
| R-002 | Broad permissions expose candidate, HR, salary, CV, client, document, or commercial data. | Confidentiality breach and loss of client trust. | Deny-by-default server authorization, record scopes, least privilege, audit logs, and security reviews. | Active |
| R-003 | Client requirements are simplified or misrepresented during architecture work. | Wrong workflows, missing entities, and expensive rework. | Treat questionnaire results and reviewed requirements as confirmed facts; use blocking PR reviews for mismatches. | Active |
| R-004 | Existing candidate and document imports contain duplicates, inconsistent data, or unsafe files. | Corrupted records, privacy exposure, failed migration. | Build staged import, duplicate detection, validation reports, administrator approval, and protected file handling. | Planned mitigation |
| R-005 | External integrations depend on unavailable APIs, licenses, tenant permissions, or provider limitations. | Blocked functionality and schedule changes. | Implement adapters, verify provider access before coding, and keep core workflows functional without integrations. | Active |
| R-006 | File storage or download design exposes CVs and HR documents. | Serious confidentiality incident. | Protected storage keys, authorized download endpoints, no public URLs, audit downloads, and future malware scanning. | Active |
| R-007 | Workflow states become inconsistent across documentation, database enums, API logic, and UI. | Broken reporting and invalid business transitions. | Keep one vocabulary, shared contracts, explicit state machines, transition tests, and documentation reviews. | Active |
| R-008 | A single-developer implementation becomes too complex to maintain. | Slow delivery and fragile code. | Modular monolith, shared standards, automated quality gates, small tasks, and documented decisions. | Active |
| R-009 | Repository memory becomes stale or contradictory. | Codex and humans act on outdated assumptions. | Mandatory status and handoff updates, source-of-truth hierarchy, compact living documents, and PR review. | Active |
| R-010 | Real client data is accidentally committed to GitHub. | Permanent data exposure through Git history. | Explicit repository rules, sanitized fixtures only, `.gitignore`, secret scanning, and review of imports and attachments. | Active |
| R-011 | Physical database constraints diverge from the approved domain model as implementation begins. | Broken workflows, missing history, and difficult migrations. | Review Prisma schema against `docs/domain-model.md`, run relational integration tests, and document implementation deviations in the domain model. | Active |
| R-012 | Prisma leaks across monorepo package boundaries or generated client state depends on pnpm `node_modules` layout. | Frontend/contracts become coupled to server persistence and builds become non-deterministic. | API-owned explicit generated output, ignored generated files, `pnpm check:architecture`, and CI clean-regeneration checks before typecheck, tests, and build. | Active |
| R-013 | Authentication tokens, password material, or audit metadata are mishandled. | Account compromise, session theft, or confidential-data leakage. | Argon2id password hashes, short-lived access tokens, hashed rotating refresh sessions, HTTP-only refresh cookies, no browser storage for tokens, generic login failures, reuse detection, safe audit summaries, and authentication integration tests. | Active |
| R-014 | Internal user administration can accidentally lock out all administrators or expose credential/session internals. | Loss of administrative recovery path or account/session compromise. | Permission-code route guards, transaction-protected last active `SUPER_ADMIN` invariant, self-lockout prevention, atomic suspension/archive session revocation, safe DTOs, safe audit summaries, and PostgreSQL invariant tests. | Active |
| R-015 | Client CRM row-scope assumptions grant broad organization or contact access before team, assignment, or optional future client-portal scoping exists. | One client, team, or employee could see another party's confidential client or contact data. | Issue #15 seeds broad client CRM permissions only to explicitly authorized internal roles, denies unresolved scoped roles by default, verifies nested contact ownership, gates commercial fields separately, serializes client archival and dependent writes on the parent client row, and tests IDOR/lifecycle/concurrency behavior against PostgreSQL. | Active |
| R-016 | Candidate profile row-scope assumptions grant broad candidate or sensitive compensation/consent access before mission, team, assignment, guest, or optional future client-portal scoping exists. | Candidate, HR, salary, or consent data could be exposed to unauthorized users. | Issue #17 seeds broad candidate/profile permissions only to explicitly authorized internal roles, gives candidate compensation and consent permissions only to `SUPER_ADMIN` by default, verifies nested profile ownership, redacts structured profile arrays from every candidate detail and mutation response when `candidate_profile:view` is absent, serializes candidate archival and dependent writes on the parent candidate row, and tests authorization, IDOR, lifecycle, response redaction, and concurrency behavior against PostgreSQL. | Active |
| R-017 | Candidate duplicate prevention is too strict or too weak for real migrated data. | Real candidate records could be blocked during import or accidentally duplicated without review. | Issue #17 keeps the existing global normalized-email uniqueness constraint, rejects duplicates without automatic merging, documents the stricter implementation choice, and defers import duplicate-review and merge workflows to later scoped work. | Active |
| R-018 | Mission row-scope assumptions or assignment races grant broad mission, salary, commercial, or client recruitment access before assignment, team, or optional future client-portal scoping is complete. | Unauthorized users could see confidential client requirements, salary ranges, commercial terms, or mutate terminal missions. | Issue #19 seeds broad mission permissions only to explicitly authorized internal roles, gives mission commercial permissions only to `SUPER_ADMIN` by default, verifies nested assignment ownership, serializes mission lifecycle/assignment writes on the parent mission row, re-checks assignee eligibility for assignment activation and lead selection, validates effective salary ranges for partial updates under the parent lock, enforces active duplicate and lead uniqueness with PostgreSQL indexes, and tests authorization, IDOR, lifecycle, protected commercial fields, and concurrency behavior against PostgreSQL. | Active |
| R-019 | Mission-candidate process scope, pipeline, or race handling could expose candidates to clients too early, duplicate applications, or count placements incorrectly. | Candidate confidentiality, reporting accuracy, and client trust could be damaged. | Issue #21 keeps linking internal-only until explicit presentation, enforces permanent `(missionId, candidateId)` uniqueness, uses responsible-recruiter ownership, redacts live candidate compensation and consent fields by permission, and serializes process writes through the D-033 lock order. Issue #29 counts placements only through offer-backed `MissionPlacement` confirmation and blocks the retired legacy integration route from counting independently. PostgreSQL tests cover duplicate creation, IDOR, presentation, protected redaction, placement counting, legacy-route bypass, and mission/candidate archival races. | Active |
| R-020 | Interview and evaluation records could expose confidential internal scoring, risks, comments, client feedback, or candidate salary context if treated as ordinary process data. | Candidate confidentiality, HR decision quality, and client trust could be damaged. | Issue #23 keeps evaluations as structured business records, gates internal evaluation content behind `evaluations:internal:view`, gates client-authored feedback behind `client_feedback:view`, omits raw candidate salary values from evaluation responses and audit metadata, uses safe audit summaries, and tests redaction and lifecycle behavior against PostgreSQL. | Active |
| R-021 | Public opportunity pages expose unapproved mission, client, salary, commercial, recruiter, pipeline, application-count, or internal data. | Candidate trust, client confidentiality, and commercial confidentiality could be damaged. | Issue #27 uses separate public DTOs, explicit public field configuration, hidden client/salary defaults, no public internal IDs where avoidable, and PostgreSQL-backed confidentiality tests. | Active |
| R-022 | Public candidate CV or file submissions overwrite older candidate files instead of preserving version and opportunity-submission history. | Recruitment evidence, auditability, and candidate history could be lost. | Issue #27 stores each public upload as a private `CandidateDocumentVersion` plus `PublicCandidateApplicationFile` trace record tied to the candidate, opportunity, submission, mission, and exact mission-candidate process. | Active |
| R-023 | Commercial operational accounting is overbuilt into full statutory accounting, under-modeled as files only, or made unreliable through source/archival side effects. | Delivery could stall, or revenue, balances, profitability, durable history, and payment tracking could become unreliable. | Issue #25 scopes commercial accounting to operational records and explicitly excludes full legal accounting, general ledger, tax declarations, bank reconciliation, and balance-sheet behavior until separately approved. Issue #38 implements quotations, commercial contracts, purchase orders, and invoices as structured business records with server-calculated totals, source-scope authorization, hidden-ID masking, exact source invoice snapshots, historical read preservation after parent archival, and placement-backed invoice eligibility while leaving payments, balances, expenses, and profitability for later scope. | Active |
| R-024 | Offer acceptance, placement confirmation, correction, or commercial eligibility could be conflated. | Mission placement counts, closure eligibility, future invoicing, and future payroll/accounting records could become inaccurate. | Issue #29 separates offer acceptance from placement confirmation, makes confirmation/correction idempotent under PostgreSQL locking, preserves original confirmation metadata, and documents Moroccan payroll as future scope only. | Active |
| R-025 | Internal task visibility, mentions, reminders, or notification summaries could expose confidential candidate, HR, salary, client, or commercial data. | Staff could see unrelated sensitive work, or duplicate reminder workers could create confusing or repeated notifications. | Issue #31 requires base `tasks:view` or `tasks:view_all` plus record scope for internal task visibility, keeps `tasks:view_all` explicit, authorizes linked context before task writes, rejects mentions/reminders for users without task access, redacts inaccessible task links in notification shaping, uses safe notification/audit summaries, and processes reminders with locked state transitions plus composite reminder and event-scoped notification idempotency. | Active |
| R-026 | Document UUID, version IDs, storage keys, archived context records, misleading visibility values, or unsafe file metadata could bypass document authorization. | CVs, contracts, HR documents, and client files could be exposed through IDOR, unsafe download paths, external access leakage, or spoofed uploads. | Issue #35 adds exact document operation permissions, reusable database-level list visibility, service-level detail/version/download/mutation access re-checks, context-specific linked-record scope checks, owner-only private/assigned visibility semantics, no external/client access from `CLIENT_SHARED`, server-generated storage keys, safe DTOs that omit storage identities, authorized download endpoints, atomic mutation/audit writes, strict base64 and raw-size bounds, MIME/extension/signature validation including bounded DOCX/XLSX OOXML ZIP-package validation, separate safe original filename metadata and sanitized download filenames, and PostgreSQL-backed regression coverage. | Active |
| R-027 | Training identifiers, nested training routes, or shared training records could expose unrelated client, HR, or participant data, and concurrent training writes could create contradictory schedule or enrollment state. | Client confidentiality could be broken through a training UUID, participants could be double-enrolled, or a session could end in a contradictory terminal state. | Issue #37 combines explicit training capabilities with server-side record scope, keeps `training_programs:view_all` a separate broad-oversight capability, hides client-linked programs from actors without `clients:view` in both the list predicate and the detail path, verifies the whole parent chain on nested session/enrollment/participation routes, requires the participation enrollment and session to share a program, enforces active enrollment and session participation uniqueness with PostgreSQL constraints, serializes mutations through a fixed program/session/enrollment/participation lock order, writes audit entries inside the mutating transaction, and covers lifecycle, IDOR, concurrency, scope, and audit-integrity behavior with PostgreSQL regression tests. | Active |
| R-028 | Parallel Prisma-heavy agent work shares one local PostgreSQL service, so one agent's `migrate reset` can silently destroy another agent's local validation database. | Local validation results can become misleading, and an agent may believe its migration chain applied when it no longer exists. | Issue #37 validated its chain on a dedicated local database instead of the shared development database, re-ran the full clean-database migration, double seed, and integration suite there, and kept its migration additive and uniquely timestamped so it never edits or reorders another feature's migration. | Active |

| R-030 | Recruitment reporting aggregates, drilldowns, or exports could leak data outside the actor's record scope or become an aggregate side channel that reveals hidden clients, missions, recruiters, or confidential fields. | Confidentiality breach across missions/clients and loss of trust. | Issue #36 constrains every count, distribution, trend, breakdown, drilldown row, and CSV row to the actor's authorized mission scope (broad requires `mission_candidates:transfer`, otherwise active `MissionRecruiter` assignment); filters only narrow scope; out-of-scope or unknown filter IDs return identical empty results and status codes without disclosing existence; protected salary/compensation/commercial/evaluation/internal-note/storage/secret fields are never included; CSV export requires a dedicated permission, is bounded and deterministic, neutralizes formula injection, and is audited with safe metadata; PostgreSQL-backed tests cover scope, IDOR, side-channel, date, and CSV behavior. | Active |

| R-029 | Training enrollment could become an alternate UUID oracle into Candidate, ClientContact, or other source domains, letting a training-only actor discover or link confidential CRM records. | Candidate and client confidentiality could be broken through a module that was never intended to grant CRM visibility. | Issue #37 requires each participant type's own source-domain read authorization before accepting its identifier, fails closed with a single identical response when that capability is absent so existence cannot be probed, redacts source identifiers from enrollment reads for actors without that visibility, and covers training-write-without-source-read, source-read-without-training-write, indistinguishable-rejection, and redaction behavior with PostgreSQL regression tests. | Active |

| R-031 | Accounting could over-allocate a payment, double-settle an invoice, mix currencies into a misleading total, or disclose financial amounts to actors who may only see the underlying business records. | Client balances, receivables, and profitability could become financially wrong, or confidential commercial amounts could leak through accounting endpoints and aggregates. | Issue #39 locks payment then invoice in a fixed order before any balance arithmetic, enforces positive amounts and active-allocation-key consistency with PostgreSQL check constraints, allows at most one ACTIVE allocation per payment/invoice pair through a unique index, supports idempotent replay through an allocation idempotency key, derives settlement instead of storing it, keeps every aggregate separated per currency with no FX, requires the accounting capability plus `commercial_data:access` plus client/mission record scope, fails aggregates closed rather than returning redacted shells, returns one indistinguishable not-found envelope for hidden and nonexistent identifiers, and covers allocation arithmetic, settlement states, concurrency races, redaction, scope, and audit integrity with PostgreSQL regression tests. | Active |

| R-032 | Generated business outputs could become a second source of truth, execute caller-controlled template content, expose confidential records through a leaked document identifier, silently alter authoritative text, commit bytes rendered from a stale source state, leave the database pointing at missing bytes, or consume excessive resources on long unbroken values. | Client-facing files could contradict the authoritative record, a hostile business value could be interpreted at render time, a commercial record or training enrollment could leak through a document UUID, a download could fail permanently, or document generation could stall on valid bounded input. | Issue #49 publishes every output as an immutable `DocumentVersion` derived from one authoritative snapshot, keeps templates code-owned with no template language, markup, evaluation, or remote fetch, sanitizes every string through one boundary, uses pure-JavaScript renderers with no native binary or shell, requires `documents:generate` plus the source domain's own authorization, re-authorizes the underlying source on detail, version listing, and current and historical downloads, mirrors that rule in the list predicate, and renders then publishes then commits with a documented compensation that deletes only the object the failed attempt published. Every version records a `sourceSnapshotSha256` that is recomputed inside the publishing transaction so a stale render is rejected, every rendered row is share-locked inside the publishing transaction so the accepted state holds until commit, the renderer never truncates or substitutes authoritative text, embeds repository-owned Noto faces with real Arabic shaping and UAX #9 ordering, and refuses a script no bundled face covers instead of corrupting it, and reading a generated certificate re-applies the participant's own source-domain capability. Issue #58 adds bounded, grapheme-safe unbroken-token line breaking that retains measured acceptance under non-monotonic shaping without timeout increases, retries, skips, or truncation. PostgreSQL check constraints keep source, taxonomy, identity, and provenance consistent, and regression tests cover source correctness, lifecycle rejection, authorization and indistinguishability, template safety, versioning, idempotency, concurrency, failure compensation, produced bytes, lossless token splitting, shaping edge cases, and bounded measurement work. | Active |
| R-033 | UI styling can drift into arbitrary raw values, inconsistent density, inaccessible contrast/focus or color-only states, noisy live-region announcements, permission-disclosing navigation, clipped controls, or an unauthenticated preview that accidentally becomes a production surface. | HireMe could lose visual coherence, keyboard and screen-reader accessibility, expose unavailable capabilities, hide required session actions, or weaken product boundaries as screens are migrated. | Issue #52 centralizes semantic tokens, contrast checks, accessible primitives, permission-aware AppShell navigation, and synthetic previews excluded from production. Issues #56, #60, and #62 apply those foundations to Reporting, Candidate, and Public Opportunity with permission-hidden actions, safe state handling, and bounded responsive layouts. Issue #64 applies them to Tasks with a keyboard-selectable status board, text-bearing status/priority/due states, labeled forms with native validation, permission-gated operational actions, one announced feedback region, request sequencing, and a real-component synthetic preview excluded from production. Later module rollouts remain behind separate visual gates. | Active |

| R-034 | Localization drifts into translated backend values, silently missing French strings, layout that breaks under longer French text, or an untrusted locale value reaching module resolution. | Business logic could branch on display text, a French screen could ship half in English, the shell could clip or overflow, or a stored value could influence what code is loaded. | Issue #54 keeps API, database, and contract values language-neutral, uses a typed canonical English dictionary with French structural parity, validates locale values against a static allow-list, keeps translations as React-escaped plain text, and formats through `Intl`. Issue #64 adds typed Task status/priority presentation mappings, sends stored enum values to APIs, removes only Tasks from the English boundary, proves same-mount switching does not refetch or clear the open create form, and verifies required responsive widths in both languages. | Active |
| R-035 | The public application form sent the salary figure a candidate types unchanged as `salaryExpectationCents`, while internal screens read that field as minor units and divide by 100. | A candidate's salary expectation could be stored and shown 100 times smaller than intended, which could mislead recruiters. | Future submissions are corrected by Issue #73 (D-066): the form holds a human major-unit amount under its own control name, converts it on the digit string through the neutral `apps/web/src/money` helper (no float multiplication, at most two decimals, point or comma, nonnegative, zero valid, empty omitted), and the submit contract bounds `salaryExpectationCents` to the `integer` maximum so an oversized amount is a 400 instead of a database error. Web, contract, and PostgreSQL regressions prove exact cents on the request, the application snapshot, and a new Candidate, and that an existing Candidate's compensation is still not overwritten. **Already-stored rows are deliberately not corrected**: persistence records no client, form version, or unit marker, and the public field has always been named `salaryExpectationCents`, so no heuristic can prove the original unit and a blanket multiplication would corrupt every already-correct row. Legacy treatment is the reviewed read-only statement `apps/api/diagnostics/public-application-salary-unit-review.sql` with `docs/runbooks/public-application-salary-unit-review.md`; it exposes no amount or currency, writes nothing, and only groups records for human review. It also asserts no new-versus-existing Candidate provenance, because nothing persisted proves it and the metadata an earlier draft relied on (`Candidate.source`, `Candidate.sourceDetail`, `PublicOpportunity.publicSlug`) is editable after the fact. | Prevented for new submissions through Issue #73; historical values remain unproven and under human review |
| R-036 | Task reminders are only delivered when someone triggers the existing processor endpoint; no background scheduler exists yet ("background-job technology" is an open technical question). | Scheduled reminders and overdue notices can arrive late or not at all if no manager runs delivery, so users may miss due work. | The processor itself is durable, row-locked, and idempotent (Issue #31). Issue #64 keeps the endpoint unchanged and moves the manual trigger into a collapsed diagnostic for managers holding `tasks:reminders:manage` and `tasks:view_all`, stating that no scheduler is configured. A scheduler (cron or worker) calling the same processor is required before production use and needs its own issue. | Open, needs scheduler issue |
| R-037 | The Task people lookup (`GET /v1/tasks/user-options`) could grow into a broad staff directory or expose account internals. | Names and work emails of internal staff could be enumerated by actors with no need for them, or role, permission, session, and sign-in data could leak. | Issue #64 limits it to the fields a selector needs (`id`, `displayName`, `email`), active internal non-archived users, at most 20 results, and requires the permission of the write each purpose prepares; mention and reminder options are also limited to users who can already view the task. A PostgreSQL integration test asserts field shape, eligibility, purpose permissions, and not-found indistinguishability. The separate filter-only lookup (`GET /v1/tasks/filter-user-options`) needs only `tasks:view` but lists only owners or active assignees of tasks visible to the actor, through the list's own visibility predicate, with the same fields and bound; its PostgreSQL test proves hidden-task people, removed or archived assignments, and other internal users never appear, and that it grants no assignment capability. | Active |
| R-038 | Candidate `source` is free text with no approved vocabulary, so filtering by an operator-recorded source depends on how it was typed. | Similar sources recorded with different spellings are filtered separately, which makes source reporting unreliable. | The platform's own `public_application` value is offered and labelled; any other source is matched exactly (ignoring case) as recorded, and no category is invented (D-063). A controlled source vocabulary needs a product decision and a data migration. | Active |
| R-039 | Candidate create and update responses include compensation and consent for actors holding the view permissions, but only list and detail reads record `candidates.compensation.viewed` / `candidates.consent.viewed`. | A sensitive value returned by a write is not separately audited as an access, so the access trail in the audit log is incomplete for that response. | Pre-existing since Issue #17 and unchanged by Issue #69, which keeps its audit correction to updates. The Candidate web re-reads the candidate after every restricted write, so each value an operator sees there is also covered by a detail-read `viewed` event, and write-only actors receive no values. A write-response access event needs its own decision. | Open, needs decision |

| R-040 | A record identifier in Reporting navigation could become an authorization bypass, stale-selection leak, or browser-history disclosure. | A guessed UUID could expose a hidden Candidate/Mission, a late response could show another target or prior principal's data, or confidential values could enter URL/history. | Issue #71 treats bounded route-specific UUIDs as navigation intent only; renders links only with the route capability; reuses the existing server-authorized detail reads and generic hidden/not-found state; places no business data beyond the identifier in the URL; clears/replaces intent on normal or manual navigation; and guards target and session generations, including React Strict Mode replay. Every awaited nested Mission read re-checks request, target, token, and permission ownership and returns stale chains before later work can start; deterministic tests include Authorization evidence. Client/process/recruiter links stay deferred where those safety conditions are not met. | Active |

## Risk protocol

- Update the register when likelihood, impact, mitigation, or ownership materially changes.
- Add newly discovered risks before beginning work that depends on them.
- Link implementation issues for concrete mitigations when they are created.
- Remove a risk only when it is no longer relevant; otherwise mark it mitigated or accepted.
- Never include confidential evidence or live personal data in the register.
